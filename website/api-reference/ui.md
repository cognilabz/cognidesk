# @cognidesk/ui

Prebuilt UI components for Cognidesk chat interfaces.

!!! info "Auto-generated from source"
    The complete type reference below is generated from TypeScript declarations. Run `pnpm docs:generate` to regenerate.

## Overview

The UI package provides pre-styled components that work with the React package. Components use Tailwind CSS with precompiled styles — no Tailwind setup required in your project.

## Appearance system

Components accept an `appearance` prop for customization via CSS variables and element class overrides.

---

## Full type reference

--8<-- "api-reference/generated/ui/dist.md"
