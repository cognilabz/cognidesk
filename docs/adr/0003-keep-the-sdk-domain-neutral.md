# Keep the SDK domain-neutral

Cognidesk may reuse architectural ideas from X, but it must not contain specific domain behavior, prompts, flows, integrations, terminology, fixtures, or UI assumptions. This keeps the SDK portable for customer support agents across tenants and industries instead of turning the reference implementation into a hidden product constraint.
