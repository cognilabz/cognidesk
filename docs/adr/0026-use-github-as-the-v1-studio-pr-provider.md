# Use GitHub as the v1 Studio PR provider

Studio Code Change Publication will use GitHub pull requests as the only v1 source-control publication provider. The implementation should make GitHub workflows good, including branch creation, draft or ready pull requests, validation summaries, labels or reviewers when configured, and links back to Studio Operator Session evidence, while leaving GitLab and Bitbucket as future provider integrations.
