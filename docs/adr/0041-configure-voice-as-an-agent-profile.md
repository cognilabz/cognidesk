# Configure voice as an agent profile

Cognidesk will configure voice through an Agent-level Voice Profile rather than a separate `createVoiceAgent`-style definition. This keeps voice attached to the same Runtime SDK Agent, Base Agent, Journeys, Tools, Knowledge, and policies while giving developers a dedicated place for voice model configuration, spoken instructions, connection adapter eligibility, recording policy, and voice interaction behavior.
