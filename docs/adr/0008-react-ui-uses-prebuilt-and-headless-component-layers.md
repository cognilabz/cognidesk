# Use prebuilt and headless React component layers

Cognidesk React will provide easy prebuilt components such as `ChatWidget` alongside lower-level headless or composable components and hooks. Styling should follow a Clerk-like developer experience with typed Appearance Configuration, theme variables, stable Element Keys, per-component overrides, and escape hatches for custom Widget renderers instead of requiring consumers to fork default components.
