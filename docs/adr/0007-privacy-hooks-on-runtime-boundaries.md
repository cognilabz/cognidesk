# Provide privacy hooks on runtime boundaries

Cognidesk will include app-defined Privacy Hooks for persistence, model inputs, traces, logs, and external adapter calls from the first version. This keeps the SDK domain-neutral while acknowledging that customer support conversations commonly contain sensitive data and that redaction policy must be controlled by the application, not hardcoded into the SDK.
