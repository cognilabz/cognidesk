# Keep a server-side voice control channel

Cognidesk's WebRTC voice adapter will keep a server-side Voice Control Channel attached to the live realtime call while the browser handles microphone and speaker media. The control channel lets Cognidesk observe provider events, route tool and knowledge projections through SDK policy, commit finalized transcripts, record interruptions, attach recording references, and emit Runtime Events without delegating runtime ownership to the browser.
