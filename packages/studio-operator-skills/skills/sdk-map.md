# Cognidesk SDK Map

The target agent is a compiled Cognidesk agent. Inspect:

- Agent instructions and behavior settings.
- State Machine Journeys for deterministic runtime checkpoints.
- Delegation Journeys for open prompt-based specialist work.
- Tools, Knowledge sources, widgets, custom events, journey events, runtime
  snapshots, and runtime events.

Use compiled journey graphs for UI and explanations. Do not infer missing
states from source code when introspection already exposes the compiled graph.
