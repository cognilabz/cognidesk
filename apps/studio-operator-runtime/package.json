{
  "name": "@cognidesk/studio-operator-runtime",
  "version": "0.0.3",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsup src/index.ts --format esm --sourcemap",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cognidesk/studio-contracts": "0.0.12",
    "@cognidesk/studio-operator-skills": "0.0.12",
    "tsx": "^4.20.6",
    "ws": "^8.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "README.md",
    "Dockerfile",
    "src",
    "tsconfig.json",
    "!src/**/*.test.ts",
    "!src/**/__tests__"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cognilabz/cognidesk",
    "directory": "apps/studio-operator-runtime"
  }
}
